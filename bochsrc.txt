# Defines how much ram we're going to have
megs: 32

# Defines where the ISO is
ata0-master: type=cdrom, path="os.iso", status=inserted

# Boot order
boot: cdrom

# Logs
log: bochsout.txt

# Configure which graphics library to use in the emulator
display_library: sdl2

# Disables mouse
mouse: enabled=0